# 测试与打包规则

- 常规功能、场景、UI、交互和 EditMode 验证优先使用 Unity 编辑器完成。
- 能通过 Unity 编辑器 Play Mode、编辑器工具或测试框架可靠覆盖的内容，不默认单独执行自动打包。
- 仅在编辑器无法可靠覆盖以下场景时打包：设备或平台差异、真实多机联机、发布包启动、Addressables 正式内容以及发布性能验收。
- 需要打包时，必须由任务明确触发，并记录构建原因、输出位置和验收结果。
- 编辑器验证与打包验证分开报告；打包成功不能替代 Unity Play Mode 或设备验收。
