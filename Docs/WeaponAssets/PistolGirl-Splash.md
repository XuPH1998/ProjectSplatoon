# PistolGirl 窄域标记枪式主武器

2026-09-17。仅调整 PistolGirl 主武器及逐发动画。核心参考 [Splash-o-matic 参数](https://sendou.ink/params/splash-o-matic)；米制射程、弹道、伤害衰减时间和涂墨尺寸是本项目适配值。

## 当前配置

武器资产：`Assets/GameResource/Weapons/PistolGirl/PistolGirlWeaponConfig.asset`。

| 项目 | 数值 |
|---|---|
| 模式、射速 | 全自动，12 发/秒，60 Hz 模拟每 5 tick 一发 |
| 伤害 | 28 → 14，弹龄 1/6 秒开始衰减，1/3 秒到最低伤害 |
| 精度 | 地面、空中、持续射击、落地均 0° 散布 |
| 耗墨 | 每发 0.8，末发后锁定回墨 1/3 秒 |
| 最大伤害射程 | 9 米，弹丸超过此距离仍可落墨，但不再造成直接伤害 |
| 射击移速 | 3.6 米/秒 |
| 弹道 | 初速 35 米/秒，直行 0.075 秒，随后 8/60 秒内减速至 0.66 倍；重力 18 米/秒² |
| 命中墨迹半径 | 0.65–1 米 |
| 沿途墨迹半径 | 0.35–0.55 米；落墨间隔仍为 0.2 米 |
| 起手 | 人形 2/60 秒，出墨 10/60 秒 |
| 动画 | 保留逐发 Shot 表现；周期 0.075 秒，Shot 状态播放倍率约 12.888889 |

100 生命、无额外伤害、关闭自动回血的测试条件下，3/5/6/7/8/9 米预期击倒发数为 4/4/4/5/6/7。四发的发射时间差为 0.25 秒，输入起手和弹丸飞行时间另计。距离应以弹丸中心到碰撞时的位置计算，不能把目标胶囊中心距离直接当作碰撞距离。

单枪口、单颗弹丸、半自动缓存为 0。角色模型、普通移动、潜墨移动、生命和墨量上限不变。Luban、运行时公共接口、网络协议没有为本次调整扩展。角色面板自动读取武器资产。

## 回归与证据

- `PistolGirlTuningTests` 验证射速、地空零散布、停火/缺墨/死亡/取消、回墨锁定、弹道伤害分段、动画周期和平地/墙面涂墨。
- `PistolGirlPlayTests` 在真实 Editor Host 加载 PistolGirl，检查连续动画和飞行粒子，并通过 `InkProjectileService` 的实际物理扫掠与服务器 `ReceiveDamage` 测量击倒发数和超射程零伤害。
- `Tools/ValidationData/WeaponAssets/PistolGirl-SplashTuning.json` 分别保存本次批准的 17 项数值覆盖，以及早于本轮已存在、要求保留的初速和直行时间。原始迁移记录不改写；依赖旧 PistolGirl 的半自动测试使用临时配置。
- 本轮原始输出位于 `Reports/PistolGirl/20260917/`。报告应区分静态检查、Unity 自动化、Editor Host 与真实双机；Host 通过不代表独立客户端或设备手感验收。

## 本轮结果

- 静态差异检查通过：武器恰好修改批准的 17 个字段，其余武器字段保留；表现配置和控制器各改一项。
- PistolGirl 资产回归通过：`python Tools/CombatGirls/validate_weapon_assets.py --hero 4` 比较 89 项历史值及明确记录的当前预期；两个历史基线覆盖本次调整后均与 PistolGirl 资产一致。
- Unity 定向测试通过 15 项：`Reports/PistolGirl/20260917/acceptance-tests.xml`。伤害距离分段由实际运行时弹道函数验证，此结果尚不是玩家碰撞实测。
- 12 发固定瞄准测量：地面涂墨宽 1.625 米，墙面场景宽 1.875 米；从前方约 1 米的首段墨路计算，四邻域连通墨迹延伸至 13.6875 米。脚下墨点与枪口前方墨路分开，不把严格 x=0 中线的连续长度当作墨路连通性。
- 完整 Host 测试及后续单项复验均未通过启动：其他进行中的爆破枪配置报“爆破枪发射、制动、碰撞或爆风配置无效”，未执行到 PistolGirl 的实际碰撞和画面验收。复验见 `host-retry-tests.xml`。真实双机、独立客户端和目标设备也未验收。
- 扩展回归历史运行结果为 71/86；其中 Pistol 墨路测量已在定向复验中修正并通过。其余失败涉及其他武器调参、英雄/字段数量、协议、旧快照缓冲区和历史预期；PistolGirl 历史预期现已单独修正。其他任务仍在修改共享文件，这次旧运行不能代表最终工作区状态。历史秒制 Python 复验仍为 5/6，全资产对比首先在 DualPistolGirl 的已有变化处失败；PistolGirl 的单独对比通过。
- 为运行验证，补充了 `DualiesNormalSimulation.cs` 缺少的 `Splatoon.Networking` 引用；没有改变其模拟逻辑。所有并行任务改动保留。
