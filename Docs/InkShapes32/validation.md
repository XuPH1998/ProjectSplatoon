# 本次验证记录

日期：2026-09-14。Unity 6000.3.9f1，独立验证副本 `D:/XPHUNITY/ProjectSplatoon-InkImpactValidation-20260914`。运行时代码、图集及场景绑定已与主工程核对一致。

## 已通过

- 16 张新增 PNG：透明 Alpha、留白、圆润饱满主体和少量边缘溅射，逐张检查完成。32 格 Alpha 哈希互不相同，原 16 格的 Alpha 逐像素保留。
- 图集导入和源图一致性校验通过；TrainingGround 36 个表面、PrototypeArena 13 个表面均引用同一个正式图集。
- 90/90 个测试通过，包括真实进入 Play Mode 的主机测试。结果文件：`Reports/InkShapes32/final-tests.xml`。
- 洗牌袋测试覆盖 80 袋、2560 次选型；每袋无重复、跨袋最近四款保护、独立射手、重置回合和固定输入重现通过。
- 实际墨弹多落点选型、半径随机流和 30/144 Hz 仿真调用频率回归通过。
- GPU 逐格测试：地面／墙面 × 32 款 × 3 个旋转，包含镜像；12,582,912 个采样，最大 CPU/GPU 墨量差为 1/255。逐格快照恢复后继续异色重复绘制与未中断绘制完全一致。
- 现有 0.125m 网格 CPU/GPU 归属测试零差异；平移和坡面归属噪声一致。
- 长墙接缝和反面隔离：232,224 个表面采样通过，使用当前武器硬度 0.55。
- 36 个地图表面的累积、恢复后继续、显示及清理通过。RT 常驻 91.49 MiB，含检查点峰值 118.68 MiB，清理后归零；这些是 RT 指标，不包含共享 Texture2D。
- 五种武器真实命中后的 Mask、DisplayMask 非空，材质绑定正确；Play Mode 快照恢复前后的字节完全一致。错误内容签名的连接审批被拒绝，改变实际 Alpha 会改变内容签名。

## 独立构建与联机

Windows Development 构建成功。三个本机进程使用本次新构建，主机沿地面和墙面连续喷墨，客户端晚加入，并完成一次退出房间后重连。最终三端均为 paint sequence `614`、归属哈希 `3182564366`，所有已涂表面的原始 GPU 纹理哈希一致：

| 表面 ID | 三端共同 RT 哈希 |
|---|---|
| 1 | 70201601 |
| 5 | 2058329707 |
| 22 | 837154536 |

本次运行没有 Shader error、连接失败或异常。重连客户端两次退出均确认 `paintRtBytes=0`。证据：`Reports/InkShapes32/Network/result.json` 及三个进程日志。它验证本机独立网络进程的 RPC、晚加入和重连快照链路；实体双机与跨 GPU 差异另行验收。

## 半径对照

地面和墙面共 60 组实际 GPU 测量（5 武器 × 撞击／沿途 × 最小／中间／最大）。固定第 16 款、角度、镜像和硬度。各组包围盒随半径缩放，最大／最小面积比相对半径平方比的最大误差约 0.151%。

步枪撞击示例：

| 半径 m | Mask 包围盒 px | Mask 面积 m² |
|---|---|---|
| 0.650 | 232×249 | 0.541306 |
| 0.725 | 258×277 | 0.673492 |
| 0.800 | 285×306 | 0.820236 |

这些数值以原始 Mask Alpha ≥ 128 统计，不等同于受湿润噪声影响的最终屏幕像素面积。最终材质截图也已检查，墨迹可见。

- [完整 60 组数据](radius-measurements.csv)
- [地面对照，左到右为最小／中间／最大](rifle-radius-floor.png)
- [墙面对照，左到右为最小／中间／最大](rifle-radius-wall.png)

## 验证边界

极低硬度 0.01 在长墙的狭窄过渡边缘观察到 3/255 的 CPU/GPU 墨量差，来自投影／插值浮点精度；当前武器使用 0.55，通过上述严格像素校验。0.01 的规则、满 Alpha 主体及平面累积测试仍保留，不能据此宣称所有位置、硬度和 GPU 均逐字节一致。

实体双机、目标设备视觉和本次 720p 四人性能对照尚需单独验收。本机多进程结果另行记录，不能替代物理设备验收。
