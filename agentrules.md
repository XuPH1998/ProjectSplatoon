# ProjectSplatoon Agent 行为规范

本文件适用于在本仓库内工作的所有 Agent。开始任何分析或修改前，必须先阅读根目录 `README.md`、本文件以及任务涉及的 `ProjectRules/` 规范。

## 一、最高优先级：受保护系统

除非用户在当前任务中明确点名授权，否则以下系统只允许阅读、检索和分析，禁止以任何形式修改。

### 1. 局域网联机框架

- 不得修改局域网建房、加入、房间码、发现服务、连接审批、传输层、网络协议、状态同步、客户端预测、房主权威判定及断线重连等框架逻辑。
- 受保护范围包括但不限于 `Assets/Splatoon/Runtime/Network/`、`Assets/Splatoon/Runtime/Prototype/` 中的网络相关代码、网络配置、协议版本、内容签名、网络测试与相应场景或 Prefab 绑定。
- 新功能必须从现有公开接口接入，不得为了接入功能改写网络框架。

### 2. 武器配置框架

- 不得修改武器配置的数据结构、加载流程、校验流程、热更新机制、Inspector 框架、配置继承或武器与弹药的绑定契约。
- 受保护范围包括但不限于武器配置相关运行时代码与编辑器代码、`Config/Luban/` 中的武器表结构、`Assets/GameResource/Weapons/` 中配置资产的结构，以及生成代码和生成数据。
- 不得通过改字段含义、默认值、单位、序列化格式或测试基线来绕过框架约束。
- 未经明确授权，也不得调整现有武器数值或武器配置资产。

### 3. 射击后涂鸦的法线、噪声等效果

- 目前不得修改射击落墨/涂鸦效果中的法线投影、表面方向、噪声、轮廓、阈值、硬度、随机旋转、镜像、UV、CPU/GPU 一致性或面积归属算法。
- 受保护范围包括但不限于 `Assets/Splatoon/Runtime/Painting/`、`SurfaceOwnershipGrid`、`InkCoverage`、涂墨 Shader/材质、相关全局参数、烘焙工具和一致性测试。
- 不得以性能优化、视觉优化、代码清理或修复边缘表现为由改变这些效果。

### 4. 玩家按 Shift 后的特殊行为逻辑

- 目前不得修改 Shift 键的输入采样、输入映射、网络同步、状态切换、移动表现、资源恢复、碰撞或动画等特殊行为逻辑。
- 当前入口包括 `Assets/Splatoon/Runtime/Prototype/PrototypePlayer.Network.cs` 中的 Shift 输入采样，所有由该输入触发的后续逻辑同样受保护。
- 不得复用、重绑定或拦截 Shift 键来实现其他功能，也不得改变该行为的触发条件和时序。

## 二、受保护系统的执行规则

- “不得修改”包括新增、删除、重命名、移动、重构文件，以及修改代码、配置、资源、场景、Prefab、材质、Shader、协议、生成器、生成物和测试期望。
- 可以进行只读调查并说明调用链、依赖和风险，但不得保存任何会改变项目行为的试验性修改。
- 若任务与受保护系统存在直接或间接耦合，必须先停止相关修改，向用户列出需要触碰的文件、原因、预计影响和最小修改范围，等待明确授权。
- 授权必须来自当前任务中的明确指令。模糊表述、历史任务许可、测试失败或“完成任务所必需”均不视为授权。
- 获得授权后也只能修改被点名的系统和范围，保持最小改动，并执行对应回归验证；不得顺带重构其他受保护内容。
- 不得通过关闭测试、删除断言、放宽校验、修改基线或手改生成物来掩盖问题。

## 三、常规工作原则

1. 修改前先检查 `git status`，识别并保留用户或其他任务已有的改动，不覆盖、不回退、不整理无关文件。
2. 先定位实际调用链、数据源和资源引用，再制定最小修改方案；不得只凭文件名或猜测修改。
3. 改动应严格限制在当前需求范围内，不进行无关重构、批量格式化、资源重导入或目录调整。
4. `Config/Luban/source` 是配置表的权威数据源。需要改表且已获授权时，应修改源表并使用既有生成流程；禁止手改 `Assets/Splatoon/Config/Generated` 和 `Assets/GameResource/Bootstrap/Config/Luban`。
5. 保持 Unity `.meta` 文件与资源 GUID 稳定。未经任务要求，不重新生成资源、不改变导入设置、不制造大范围序列化变更。
6. 使用项目现有接口、目录、命名和实现模式，遵守 `ProjectRules/` 下的网络、加载、技能、数据、测试与目录规范。
7. 不主动执行发布构建。常规验证优先使用静态检查、相关测试和 Unity 编辑器；只有任务明确要求或编辑器无法覆盖目标场景时才构建。

## 四、验证与交付

- 根据改动风险运行最小且充分的相关验证，并在结果中区分：静态检查、EditMode、PlayMode、构建、真实局域网多机和人工视觉/手感验证。
- 没有实际执行的验证不得声称通过。单机或同机多进程测试不得表述为真实双机局域网验证。
- 完成后列出实际修改的文件、行为变化、已执行验证和未验证风险。
- 若需求只能通过修改受保护系统完成，应保持项目不变并明确报告阻塞点，等待用户决定。
