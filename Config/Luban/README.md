# ProjectSplatoon Luban workspace

The current workbooks are an imported schema baseline used to prove the generator path. Before production gameplay data is authored, remove unused Vanguard tables and replace them with Splatoon-owned tables while keeping `__tables__.xlsx`, `__beans__.xlsx`, and `__enums__.xlsx` synchronized. Generated C# and JSON are rebuild outputs and must never be hand-edited.
